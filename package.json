{
  "name": "vibro",
  "version": "0.3.0",
  "private": false,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0",
    "ios:prepare": "npm run build && node scripts/prepare-ios.mjs",
    "ios": "npm run ios:prepare && cap open ios",
    "cap:sync": "cap sync"
  },
  "dependencies": {
    "@capacitor-community/bluetooth-le": "latest",
    "@capacitor/core": "latest",
    "@vitejs/plugin-react": "latest",
    "lucide-react": "latest",
    "react": "latest",
    "react-dom": "latest",
    "vite": "latest"
  },
  "devDependencies": {
    "@capacitor/cli": "latest",
    "@capacitor/ios": "latest",
    "typescript": "latest"
  }
}
